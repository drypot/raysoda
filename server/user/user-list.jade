extends ../layout/layout

block head-title
  title Users - #{appName}

block content

  script.
    $(function() {
      cuser.initUserList();
    });

  .section.page-title
    h1 Users

  .section
    form.main
      .form-group
        .form-group
          input.form-control(name='q', type='text')
      .sub-section
        button.btn.btn-primary(name='send') Search

  .section
    #result.basic-table
      | Total #{count} users.
