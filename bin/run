#!/bin/bash

function show_usage {
    echo "run [raysoda|rapixel|osoky|drypot] [dev|live]"
    exit 1
}

if [ $# -lt 2 ]; then
    show_usage
fi

if [ "$2" = dev ]; then
    OPT=dev
elif [ "$2" = live ]; then
    OPT=live
else
    show_usage
fi

node dist/main/main.js config/${1}-${OPT}.json
