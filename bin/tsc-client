#!/usr/bin/env bash

args=(
    # --build src-client/tsconfig.json
    --project src-client/tsconfig.json
    # --listEmittedFiles    # 새로 생성된 파일 목록을 출력한다. 파일 출력 때문에 에러가 안 보인다;
    # --watch
)

set -x # enable debugging output

node_modules/.bin/tsc "${args[@]}" "$@"
