#!/usr/bin/env bash

# 2025-08-05
# rollup 대신 esbuild 를 사용한다.
# rollup + rollup typescript plugin 기본 구성으로는 번들 위치를 유연하게 지정할 수가 없다.

exit

node_modules/.bin/rollup --config rollup.config.ts "$@" # --watch
