#!/usr/bin/env bash

function show_usage {
    echo "bin/raysoda-live [raysoda|rapixel|osoky|drypot]"
    exit 1
}

if [ $# -lt 1 ]; then
    show_usage
fi

export NODE_ENV=production
export NODE_OPTIONS='-r ts-node/register -r tsconfig-paths/register'
export TS_NODE_PROJECT=src/server/tsconfig.json

set -o xtrace
node src/server/ts/script/raysoda.ts config-live/${1}-live.json
