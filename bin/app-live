#!/usr/bin/env bash

function show_usage {
    echo "bin/app-live [raysoda|rapixel|osoky|drypot]"
    exit 1
}

if [ $# -lt 1 ]; then
    show_usage
fi

export NODE_ENV=production

set -o xtrace
npx ts-node src/server/script/run-app.ts config-live/${1}-live.json
