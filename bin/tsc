#!/usr/bin/env bash

args=(
#  --build tsconfig.json
  --project tsconfig.json
  --listEmittedFiles    # 새로 생성되는 파일 목록을 출력한다.
  --incremental         # 변경된 파일만 컴파일한다.
)

set -x # enable debugging output

node_modules/.bin/tsc "${args[@]}" "$@"
