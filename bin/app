#!/usr/bin/env bash


function show_usage {
    echo "bin/app [raysoda|rapixel|osoky|drypot]"
    exit 1
}

if [ $# -lt 1 ]; then
    show_usage
fi

#export NODE_ENV=development

set -o xtrace
ts-node src/server/script/run-app.ts config/${1}-dev.json
