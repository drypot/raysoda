#!/usr/bin/env bash


function show_usage {
    echo "bin/app [raysoda|rapixel|osoky|drypot]"
    exit 1
}

if [ $# -lt 1 ]; then
    show_usage
fi

BUILD_DIR=build/src
#export NODE_ENV=development

set -o xtrace
node $BUILD_DIR/script/run-app.js config/${1}-dev.json
