extends ../layout/layout

block head-opt
  title Error - #{config.appName}

block content

  .section.page-title
    h1 Error

  .section.error-report
    | #{err[0].name + '\n'}
    | #{err[0].field}
