<!DOCTYPE html>
<html lang="ko">
<head>
    <% app.title = 'Login' %>
    <%- include('../_layout/head') %>
</head>
<body>
    <%- include('../_layout/header') %>

    <div class="wrapper">
        <h1>로그인</h1>
        <form id="loginForm" class="form max-w-xs">
            <label>
                <span>이메일</span>
                <input type="text" name="email" maxlength=64 autofocus>
            </label>
            <label>
                <span>비밀번호</span>
                <input type="password" name="password" maxlength=32>
            </label>
            <label class="inline-label">
                <input type="checkbox" name="remember">
                <span>자동로그인</span>
            </label>
            <div class="btn-group">
                <button class="btn btn-blue" name="send">로그인</button>
            </div>
        </form>
        <p class="mt-xl flex-center flex-column space-y">
            <a href='/user-register'>회원가입</a>
            <a href='/user-password-mail'>비밀번호 초기화</a>
        </p>
    </div>

    <%- include('../_layout/footer') %>
    <script>
        app.initUserLoginForm()
    </script>
</body>
</html>
