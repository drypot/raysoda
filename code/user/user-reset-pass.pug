extends ../layout/layout

block head-opt
  title Reset Password - #{appName}

block content

    .section.page-title
      h1 비밀번호 초기화

    if !query.step
      script.
        $(function () {
          cuser.initResetPassStep1();
        });

      .section
         form.main
          .sub-section
            .form-group
              label.control-label 이메일
              input.form-control(name='email', type='text')
          .sub-section
            button.btn.btn-primary(name='send', data-loading-text='전송중') 비밀번호 초기화

    if query.step == 2
      .section
        | 메일을 확인해 주십시오.

    if query.step == 3
      script.
        $(function () {
          cuser.initResetPassStep3();
        });

      .section
        form.main
          .sub-section
            .form-group
              label.control-label 새 비밀번호
              input.form-control(name='password', type='password')
          .sub-section
            button.btn.btn-primary(name='send', data-loading-text='전송중') 등록
