<!DOCTYPE html>
<html lang="ko">
<head>
    <% locals.title = 'Password Reset' %>
    <%- include('../common/head') %>
</head>
<body>
    <%- include('../common/header') %>

    <div class="wrapper">
        <h1>비밀번호 초기화</h1>
        <p>비밀번호 초기화 메일을 보내드립니다.</p>
        <form id="form" class="max-w-2xs">
            <label>
                <span>이메일</span>
                <input type="email" name="email" maxlength=64 autofocus>
            </label>
            <div class="error-group"></div>
            <div class="btn-group">
                <button class="btn-blue" name="send">메일 발송</button>
            </div>
        </form>
    </div>

    <%- include('../common/footer') %>
    <script>
        page.initUserPasswordMailForm()
    </script>
</body>
</html>
